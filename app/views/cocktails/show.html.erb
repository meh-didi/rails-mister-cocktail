<div class="container mt-5">
<h1> <%= @cocktail.name%></h1>
<p>Recipe</p>
<% @cocktail.doses.each do |dose| %>
<ul>
  <%= dose.description %> dose de

<%= dose.ingredient.name %>

</ul>


<div>
  <% end %>
  <%= link_to "Back", cocktails_path, class: "btn btn-primary mt-2" %> <%= link_to "Add a dose", new_cocktail_dose_path(@cocktail), class: "btn btn-primary mt-2" %></div>
</div>
